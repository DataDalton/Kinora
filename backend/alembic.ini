[alembic]
# Path to migration scripts
script_location = app/alembic

# Template used for generating new migrations
file_template = %%(year)d%%(month).2d%%(day).2d_%%(hour).2d%%(minute).2d_%%(rev)s_%%(slug)s

# Prepend sys.path with the project root
prepend_sys_path = .

# Timezone to use when rendering the migration datetime
timezone = UTC

# Max length of characters to apply to the slug field
truncate_slug_length = 40

# Set to 'true' to run the environment during autogenerate
revision_environment = false

# Set to 'true' to allow .pyc and .pyo files without source
sourceless = false

# Version file locations; defaults to %(here)s/versions
version_locations = %(here)s/app/alembic/versions

# Output encoding used when writing scripts
output_encoding = utf-8

# SQLAlchemy URL is set via environment variable in env.py
# sqlalchemy.url = driver://user:pass@localhost/dbname

[post_write_hooks]
# Black formatting after migration generation
# hooks = black
# black.type = console_scripts
# black.entrypoint = black
# black.options = -q

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
